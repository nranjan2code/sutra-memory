┌──────────────────────────────────────────────────────────────────┐
│                 SUTRA AI - QUICK DEPLOYMENT GUIDE                │
│                    (For Small Teams)                              │
└──────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════
  🚀 THE ONLY COMMANDS YOU NEED
═══════════════════════════════════════════════════════════════════

┌─ SINGLE SERVICE UPDATE (30 seconds!) ─────────────────────────┐
│                                                                 │
│  ./sutra-deploy.sh update sutra-api       # Update API         │
│  ./sutra-deploy.sh update sutra-client    # Update frontend    │
│  ./sutra-deploy.sh update sutra-hybrid    # Update hybrid      │
│                                                                 │
│  ✅ Only rebuilds the service you changed                      │
│  ✅ Other services keep running                                │
│  ✅ 10x faster than full rebuild                               │
└─────────────────────────────────────────────────────────────────┘

┌─ DEVELOPMENT MODE (Instant updates!) ─────────────────────────┐
│                                                                 │
│  # Start once:                                                  │
│  docker-compose -f docker-compose-grid.yml \                   │
│                 -f docker-compose.dev.yml up                    │
│                                                                 │
│  ✅ Python changes apply instantly (no rebuild!)               │
│  ✅ React auto-refreshes browser                               │
│  ✅ Perfect for daily coding                                   │
└─────────────────────────────────────────────────────────────────┘

┌─ SEE WHAT CHANGED ────────────────────────────────────────────┐
│                                                                 │
│  ./scripts/detect-changes.sh                                    │
│                                                                 │
│  Shows exactly which packages need updating                     │
└─────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════
  📊 SPEED COMPARISON
═══════════════════════════════════════════════════════════════════

  API Code Change:
    ❌ Old way: 5-8 minutes (full rebuild)
    ✅ New way: 30 seconds (single update)
    → 10x FASTER

  Frontend Change:
    ❌ Old way: 3-5 minutes (rebuild + restart)
    ✅ New way: Instant (hot reload)
    → INSTANT

  Python Development:
    ❌ Old way: Rebuild after every change
    ✅ New way: Live reload, no rebuilds
    → NO WAITING

═══════════════════════════════════════════════════════════════════
  🎯 DECISION TREE: WHICH COMMAND TO USE?
═══════════════════════════════════════════════════════════════════

  ┌─ Are you actively developing? ───────────────────────────┐
  │ YES → Use development mode with hot reload               │
  │       docker-compose -f docker-compose-grid.yml \        │
  │                      -f docker-compose.dev.yml up        │
  └──────────────────────────────────────────────────────────┘

  ┌─ Made a small change to one service? ────────────────────┐
  │ YES → Use the update command                             │
  │       ./sutra-deploy.sh update <service-name>            │
  └──────────────────────────────────────────────────────────┘

  ┌─ Changed core dependencies or protocol? ─────────────────┐
  │ YES → Do a full rebuild (rare!)                          │
  │       ./sutra-deploy.sh build                            │
  └──────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════
  🔧 COMMON WORKFLOWS
═══════════════════════════════════════════════════════════════════

  Daily Python/JS Development:
  ──────────────────────────────────────────────────────────────
    1. Start dev mode ONCE:
       docker-compose -f docker-compose-grid.yml \
                      -f docker-compose.dev.yml up -d
    
    2. Edit code in your editor
    
    3. Changes apply automatically!
       - Python: Auto-reload
       - React: Browser refresh
    
    4. No rebuild needed! 🎉

  Quick Service Update:
  ──────────────────────────────────────────────────────────────
    1. Make your code change
    
    2. Update just that service:
       ./sutra-deploy.sh update sutra-api
    
    3. Done in 30 seconds! ✅

  Multiple Service Changes:
  ──────────────────────────────────────────────────────────────
    1. Check what changed:
       ./scripts/detect-changes.sh
    
    2. Update each service:
       ./sutra-deploy.sh update sutra-api
       ./sutra-deploy.sh update sutra-hybrid
    
    3. Each takes ~30s → Total: 1-2 min vs 10 min!

═══════════════════════════════════════════════════════════════════
  ⚡ AVAILABLE SERVICES
═══════════════════════════════════════════════════════════════════

  storage-server    - Core storage engine (Rust)
  sutra-api         - REST API service (Python)
  sutra-hybrid      - Hybrid reasoning (Python)
  sutra-client      - Web client UI (React)
  embedding-single  - Embeddings (Simple/Community)
  embedding-ha-1/2/3- HA Embeddings (Enterprise)
  grid-master       - Grid orchestration (Enterprise)
  grid-agent-1/2    - Grid agents (Enterprise)

═══════════════════════════════════════════════════════════════════
  🐛 TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════

  Changes not showing?
  ────────────────────────────────────────────────────────────────
    → Make sure you're in dev mode OR use update command

  Service failing after update?
  ────────────────────────────────────────────────────────────────
    ./sutra-deploy.sh logs <service-name>
    ./sutra-deploy.sh status

  Need a clean slate?
  ────────────────────────────────────────────────────────────────
    ./sutra-deploy.sh clean
    ./sutra-deploy.sh install

═══════════════════════════════════════════════════════════════════
  📚 DOCUMENTATION
═══════════════════════════════════════════════════════════════════

  • FAST_DEVELOPMENT.md           - Detailed dev guide
  • SIMPLE_DEPLOYMENT_SOLUTION.md - What we built & why
  • ./sutra-deploy.sh help         - All commands
  • docker-compose.dev.yml         - Dev mode config

═══════════════════════════════════════════════════════════════════
  💡 KEY TAKEAWAYS
═══════════════════════════════════════════════════════════════════

  ✅ Use update command for single service changes (10x faster)
  ✅ Use dev mode for Python/JS development (instant changes)
  ✅ Never do full rebuilds unless changing dependencies
  ✅ Check what changed with detect-changes.sh script

  🎉 You now have 80% of enterprise benefits with 20% complexity!

┌──────────────────────────────────────────────────────────────────┐
│  Questions? Check FAST_DEVELOPMENT.md or run:                   │
│  ./sutra-deploy.sh help                                          │
└──────────────────────────────────────────────────────────────────┘
